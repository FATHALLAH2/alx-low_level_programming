#include <stdlib.h>
#include <time.h>
/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{
	int n;
        a = n % 10;
	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if ( n > 48)
	{
            putchar("%d is negative",a);
	}
	return (0);
}
